% form_index ||= 0

% f.fields_for :action_tags, @action_tags do |action_tag|
  = render :partial => "action_tag_fields", :locals => { :form_index => form_index, :action_tag => action_tag }
  % form_index += 1
